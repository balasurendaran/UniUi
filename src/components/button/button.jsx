import React from "react";
import { Button } from "antd";

const UniUiButton = ({ className }) => (
  <div className={className}>
    <Button type="primary">Button</Button>
  </div>
);

export default UniUiButton;
